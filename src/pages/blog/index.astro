---
import Layout from '@/layouts/Layout.astro';
import Post from '@/features/blog/components/Post/index.astro';
import { getCollection } from 'astro:content';

const title = 'Blog';
const posts = await getCollection('blog');
---

<Layout title={title}>
  <main>
    <div>
      <h1>
        web dev content, mostly
        <br />
         updated weekly, probably
      </h1>
    </div>
    <section>
      <h2>Latest</h2>
      <ul>
        {
          posts.map((post) => {
            return <Post url={`/blog/${post.slug}`} data={post.data} />;
          })
        }
      </ul>
    </section>
  </main>
</Layout>

<style>
  main {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin: 0 auto;
  }

  h2 {
    font-size: var(--text-2xl);
  }

  section {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    padding: 2rem;
  }

  ul {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: 24rem;
  }
</style>
